# Optional extras (ingress + Prometheus ServiceMonitor). Not part of the
# base — overlays opt in as needed by referencing ../../extras.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ingress.yaml
  - servicemonitor.yaml
