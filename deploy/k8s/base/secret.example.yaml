# EXAMPLE ONLY — do not commit real secrets. Copy this file, replace the
# values, and apply it separately (or use `kubectl create secret generic
# event-watch-secret --from-literal=EW_AUTH_TOKEN=... -n event-watch`).
apiVersion: v1
kind: Secret
metadata:
  name: event-watch-secret
  labels:
    app.kubernetes.io/name: event-watch
    app.kubernetes.io/component: server
type: Opaque
stringData:
  EW_AUTH_TOKEN: "replace-me-with-a-real-token"
  EW_GITHUB_SECRET: "replace-me-with-your-github-webhook-secret"
